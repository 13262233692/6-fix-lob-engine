[package]
name = "fix-lob-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["net", "io-util", "rt-multi-thread", "macros", "sync"] }

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
